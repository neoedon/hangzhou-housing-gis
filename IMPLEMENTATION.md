# 杭州住房 GIS 实施计划

用户于 2026-09-08 同意 `GIS_项目整合方案_2026-09-08.md`。交付为本地 GIS 应用；不是 Data Analytics 的便携只读报表，因此不采用限制地图/持久化交互的报表运行时。沿用看板技能的来源、口径、全局筛选与 QA 规范。

## 工作与验收

- [x] `build_data.py`：只读原始文件，建立 SQLite 索引、稳定源记录 ID、年度招生关系、候选/挂牌/成交/原帖、历史候选快照与来源登记。歧义保留；名称匹配不等于地址核验。通过后原子替换数据库。
- [x] `server.py`：仅监听 127.0.0.1；静态资源与 API 白名单；参数化 SQL、只读数据库；不能通过路径请求工作区任意文件；拒绝混用不同构建版本。
- [x] `web/index.html`、`web/app.js`、`web/model.js`、`web/app.css`：MapLibre 单引擎地图，统一地区/类型/学校年度/预算/面积/空间/日期查询；点选学校、教育集团、小区和证据双向跳转。
- [x] 教育集团：基于滨江、拱墅官方学校档案建立 20 个集团、48 条成员关系；区分 46 条现行和 2 条历史关系，支持集团↔学校双向查看及成员校地图联动。
- [x] 地图深交互：当前视野与矩形圈选、图层和标识只显、历史快照、2—4 小区比较、本地收藏和私有置换草稿。
- [x] 来源面板：实际源日期、报告生成日期、名称关联状态、过期挂牌、成交历史样本；未来入学年份缺失时明确为空。
- [x] 测试：133 项数据/API、99 项前端状态测试；含教育集团数量、现行/历史关系、学校反向索引和集团聚合查询。桌面/窄屏实际 GIS 交互、48 个拖拽帧样本、关闭在线底图时本地研究层保留；空结果、未来年度与来源缺口明确。
- [x] Companion notebook：通过 SQLite 重跑数量、源日期、匹配覆盖与关系检查，5 个代码单元已执行。
- [x] `README.md`、构建/验收回执：启动方式、数据更新方式、原文件哈希不变、覆盖缺口与未验证项。

## 命令

`python3 housing-gis/build_data.py`

`python3 -m unittest discover -s housing-gis/tests -p 'test_*.py'`

`node --test housing-gis/tests/*.test.mjs`

`python3 housing-gis/server.py --port 8766`

实际浏览器检查使用 ego-browser 的任务空间；旧 file 页面不重试或改协议绕过。新 GIS 独立实现，只提供规范化索引/API，不转发原受限 HTML。遇到访问策略或用户接管立即停止相应浏览器路径。

## 不在本轮授权中

不重新请求小红书、不修改现有自动化线程/调度、不发送中介消息、不进行交易、不修改原始证据、不发布公网、不自动导入私人家庭身份与融资数据到地图。
